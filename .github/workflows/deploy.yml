name: c-build

on:
  workflow_dispatch:
    inputs:
      environment:
      description: 'Environment to run tests against'
      type: environment
      required: true 
jobs:
  deployToTesting:
    runs-on: RedHat8
    environment: production
    steps:
      - 
        uses: actions/checkout@v3
